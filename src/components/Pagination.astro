---
// import components
import Link from "./Link.astro";

const { pervUrl, nextUrl } = Astro.props;
---

<nav aria-label="Blog pages">
  {
    pervUrl && (
      <Link
        text="Previous"
        href={pervUrl}
        style="primary"
        borderVisible={true}
        isFilled={false}
        icon={{
          name: "tabler:arrow-big-left-line",
          side: "left",
        }}
      />
    )
  }
  {
    nextUrl && (
      <Link
        text="Next"
        href={nextUrl}
        style="primary"
        borderVisible={true}
        isFilled={false}
        icon={{
          name: "tabler:arrow-big-right-line",
          side: "right",
        }}
      />
    )
  }
</nav>
